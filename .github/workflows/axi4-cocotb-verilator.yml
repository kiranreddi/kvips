name: AXI4 Cocotb CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'axi4/**'
      - 'common/cocotb/**'
      - 'common/sv/**'
      - 'scripts/run_cocotb_verilator.py'
      - 'scripts/ensure-verilator-uvm.sh'
      - 'third_party/uvm/**'
      - '.github/workflows/axi4-cocotb-verilator.yml'
      - '.github/verilator-version'
      - '.github/actions/setup-verilator/**'
      - '.github/actions/setup-cocotb/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'axi4/**'
      - 'common/cocotb/**'
      - 'common/sv/**'
      - 'scripts/run_cocotb_verilator.py'
      - 'scripts/ensure-verilator-uvm.sh'
      - 'third_party/uvm/**'
      - '.github/workflows/axi4-cocotb-verilator.yml'

jobs:
  axi4-cocotb:
    name: AXI4 Cocotb (Verilator + DPI)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Verilator
        uses: ./.github/actions/setup-verilator

      - name: Setup cocotb
        uses: ./.github/actions/setup-cocotb

      - name: Run AXI4 cocotb suite
        run: |
          set -euo pipefail
          source .venv-cocotb/bin/activate
          python3 scripts/run_cocotb_verilator.py --protocol axi4
          ! grep -q '<failure' axi4/examples/cocotb_dut/sim/out/cocotb_verilator/results.xml

      - name: Archive Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: axi4-cocotb-verilator-results
          path: axi4/examples/cocotb_dut/sim/out/cocotb_verilator/results.xml
