name: AXI4 DUT CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'axi4/**'
      - 'common/**'
      - 'third_party/uvm/**'
      - '.github/workflows/axi4-dut-verilator.yml'
      - '.github/verilator-version'
      - '.github/actions/setup-verilator/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'axi4/**'
      - 'common/**'
      - 'third_party/uvm/**'
      - '.github/workflows/axi4-dut-verilator.yml'

jobs:
  axi4-dut:
    name: AXI4 DUT (Verilator)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Verilator
        uses: ./.github/actions/setup-verilator

      - name: Run AXI4 DUT Regressions
        run: |
          cd axi4/examples/uvm_dut/sim
          chmod +x run_verilator.sh regress_verilator.sh
          export VERILATOR_JOBS=$(nproc)
          ./regress_verilator.sh

      - name: Archive Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: axi4-dut-verilator-results
          path: axi4/examples/uvm_dut/sim/out/verilator/
