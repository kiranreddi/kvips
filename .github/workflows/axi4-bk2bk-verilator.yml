name: AXI4 BK2BK CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'axi4/**'
      - 'common/**'
      - 'third_party/uvm/**'
      - '.github/workflows/axi4-bk2bk-verilator.yml'
      - '.github/verilator-version'
      - '.github/actions/setup-verilator/**'
  pull_request:
    branches: [ main ]
    paths:
      - 'axi4/**'
      - 'common/**'
      - 'third_party/uvm/**'
      - '.github/workflows/axi4-bk2bk-verilator.yml'

jobs:
  axi4-bk2bk:
    name: AXI4 BK2BK (Verilator)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Verilator
        uses: ./.github/actions/setup-verilator

      - name: Run AXI4 BK2BK Regressions
        run: |
          cd axi4/examples/uvm_back2back/sim
          chmod +x run_verilator.sh regress_verilator.sh
          export VERILATOR_JOBS=$(nproc)
          ./regress_verilator.sh

      - name: Archive Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: axi4-bk2bk-verilator-results
          path: axi4/examples/uvm_back2back/sim/out/verilator/
